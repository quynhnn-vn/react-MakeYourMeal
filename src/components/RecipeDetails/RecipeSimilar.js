// To be updated

/*
import React, { useEffect, useState } from "react";
import { getSimilarRecipes } from "../../api/api";

export const RecipeSimilar = ({ id }) => {
  const [recipes, setRecipes] = useState([]);
  useEffect(() => {
    getSimilarRecipes(id, 3).then((recipes) => {
      setRecipes(recipes);
    });
  }, [id]);
  return (
    <div>
      <h2>SIMILAR RECIPES</h2>
      {recipes.map(recipe => (
          <h3>{recipe.title}</h3>
      ))}
    </div>
  );
};
*/
